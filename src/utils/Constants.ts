export const ROUTE_PATHS = {
  HOME: "/",
  FLIGHTLISTING: "/list",
  COMPLETED: "/complete",
};

export enum BRAND_CODES {
  ecoFly = "ecoFly",
  extraFly = "extraFly",
  primeFly = "primeFly",
}

export enum FARE_CATEGORIES {
  business = "BUSINESS",
  economy = "ECONOMY",
}

export enum STATUS {
  AVAILABLE = "AVAILABLE",
  ERROR = "ERROR",
}
